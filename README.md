# memap-core

Core functions and types for memap in-memory cache. Provides a public API for embedded Go usage without an external server.

## Installation

```shell
go get github.com/memap-project/memap-core
```
